export interface ILogin {
    email: string;
    password: string;
}

export interface IRegister {
    termAndConditionId: number;
    name: string;
    surname: string;
    email: string;
    password: string;
}
